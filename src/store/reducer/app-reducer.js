const INITIAL_STATE = {
    app_name: "BoilerPlate"
}

export default (state = INITIAL_STATE, action)=>
{
    return state;
    // console.log(action);
    // switch (action.type) {
    //     case "SETDATA":
    //         return({
    //             ...state,
    //             data: action.data
    //         })
    //         break;
    
    //     default:
    //         break;
    // }
}